import React from 'react';
import { Switch } from 'react-router-dom';

/*
 * TODO: setup routes
 *
 * <PublicRoute exact path={}><Public /></PublicRoute>
 * <PublicRoute restricted exact path={}><RestrictedPublic /></PublicRoute>
 * <PrivateRoute exact path={LESSONS_ROUTE}><Private /></PrivateRoute>
 * <Redirect from="*" to={} />
 */

const Routes = () => (
  <Switch>
  </Switch>
);

export default Routes;
